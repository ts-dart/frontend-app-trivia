export interface questionInfo {
    category: string,
    correctAnswer: string,
    difficulty: string,
    incorrectAnswers: string[],
    question: string,
    type: string,
    allAnswers: string[],
}